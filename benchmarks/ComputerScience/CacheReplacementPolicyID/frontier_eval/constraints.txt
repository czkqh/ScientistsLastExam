1) Only edit solution.py. Keep identify(problem, run).
2) Return {"verdict", "machine", "confidence"}. `verdict` is "policy" or "no_policy". When it is "policy", `machine` is a mapping with `hit`, a list of n rows each listing problem["ways"] integer next states in 0..n-1, and `miss`, a list of n pairs [victim way in 0..problem["ways"]-1, next state in 0..n-1], with 1 <= n <= problem["max_states"] and state 0 the state after reset. `confidence` in [0,1]. `abstain: true` is accepted as a synonym for the no_policy verdict.
3) run(trace) plays a non-empty list of at most problem["max_trace_length"] integer block addresses in 0..2147483647 from the reset state and returns one boolean per access, True for a hit, each flipped with probability problem["noise"]; it is charged problem["ways"] + len(trace) accesses. Do not exceed problem["run_budget"] accesses per world.
4) NumPy/SciPy only. Deterministic CPU code. No network/process creation. Do not read verification/ or frontier_eval/.
